name: ReleaseTag

on:
  push:
    tags: ['v*.*.*']

jobs:
  release-tag:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: git log -n 5
      - run: git describe --tags --exact-match
