
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.2.5
  creationTimestamp: null
  name: scalablenodegroups.karpenter.sh
spec:
  group: karpenter.sh
  names:
    kind: ScalableNodeGroup
    listKind: ScalableNodeGroupList
    plural: scalablenodegroups
    singular: scalablenodegroup
  scope: Namespaced
  subresources:
    scale:
      labelSelectorPath: .status.selector
      specReplicasPath: .spec.replicas
      statusReplicasPath: .status.replicas
  validation:
    openAPIV3Schema:
      description: ScalableNodeGroup is the Schema for the ScalableNodeGroups API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: ScalableNodeGroupSpec is an abstract representation for a Cloud
            Provider's Node Group. It implements https://kubernetes.io/docs/tasks/extend-kubernetes/custom-resources/custom-resource-definitions/#scale-subresource
            which enables it to be targeted by Horizontal Pod Autoscalers.
          properties:
            maxReplicas:
              description: MaxReplicas limits the maximum size of the ScalableNodeGroup
              format: int32
              type: integer
            minReplicas:
              description: MinReplicas limits the minimum size of the ScalableNodeGroup
              format: int32
              type: integer
            replicas:
              description: Replicas is the desired number of replicas for the targeted
                Node Group
              format: int32
              type: integer
            type:
              description: Type for the resource of name ScalableNodeGroup.ObjectMeta.Name
              type: string
          required:
          - type
          type: object
        status:
          description: ScalableNodeGroupStatus holds status information for the ScalableNodeGroup
          properties:
            replicas:
              description: Replicas displays the current size of the ScalableNodeGroup
              format: int32
              type: integer
          type: object
      type: object
  version: v1alpha1
  versions:
  - name: v1alpha1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
