apiVersion: v1
kind: Service
metadata:
  labels:
    control-plane: karpenter-controller-manager
  name: karpenter-controller-manager-metrics-service
  namespace: karpenter-system
spec:
  ports:
  - name: https
    port: 8443
    targetPort: https
  selector:
    control-plane: karpenter-controller-manager
