apiVersion: v1
kind: Namespace
metadata:
  labels:
    control-plane: karpenter-controller-manager
  name: karpenter-system
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: karpenter-controller-manager
  namespace: karpenter-system
  labels:
    control-plane: karpenter-controller-manager
spec:
  selector:
    matchLabels:
      control-plane: karpenter-controller-manager
  replicas: 1
  template:
    metadata:
      labels:
        control-plane: karpenter-controller-manager
    spec:
      containers:
      - command:
        - /manager
        args:
        - --enable-leader-election
        image: controller:latest
        name: manager
        resources:
          limits:
            cpu: 100m
            memory: 30Mi
          requests:
            cpu: 100m
            memory: 20Mi
      terminationGracePeriodSeconds: 10
