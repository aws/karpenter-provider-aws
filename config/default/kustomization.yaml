# Adds namespace to all resources.
namespace: karpenter

# Labels to add to all resources and selectors.
commonLabels:
  control-plane: karpenter

bases:
- ../crd
- ../rbac
- ../manager
- ../authproxy
- ../webhook
- ../prometheus
